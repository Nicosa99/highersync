HIGHERSYNC / SOULTUNE DELIVERY BACKEND (VPS)Role: Senior Node.js Backend ArchitectYou are tasked with building a highly efficient, standalone fulfillment backend for "Quantum Wealth" (a SoulTune audio protocol).This backend connects ClickBank (Checkout) with our customers via a self-hosted Node.js architecture on a Contabo VPS.NO external databases or expensive cloud services (like Vercel or Supabase) are allowed. Keep it lean, mean, and secure.1. TECH STACK & CONSTRAINTSEnvironment: Contabo VPS (Linux/Ubuntu)Runtime: Node.js (v18+)Framework: Express.jsProcess Manager: PM2 (for keeping the server alive)Language: TypeScriptEmails: Nodemailer (using SMTP) or a free-tier API like Resend.Storage: Local disk only. The .soultune file will be hosted directly on the VPS.2. THE DELIVERY PIPELINEWhen a user buys on ClickBank, the following sequence must execute autonomously:Receive IPN: ClickBank sends a POST request (Instant Notification) to your endpoint: /api/webhooks/clickbank.Verify Signature (CRITICAL): Calculate the HMAC-SHA1 of the payload using the ClickBank Secret Key (process.env.CLICKBANK_SECRET). If the signature from the cbits header does not match, return 401 Unauthorized.Parse Payload: Extract receipt_receipt, customer_email, and customer_first_name from the decrypted IPN JSON.Generate Secure Link: * Since email attachments >25MB often bounce (audio files are large), we host quantum_wealth.soultune securely on the VPS.Create a simple, secure download endpoint (e.g., /download/:token) where the token is a simple signed JWT containing the receipt_id, valid for 48 hours.Dispatch Email: * Send the "Access Granted" email to the customer_email.The email must contain a warm welcome message and the secure download link.Provide brief instructions: "Download this file on your phone and open it. The SoulTune app will automatically import your Quantum Wealth journey."3. APP CONTEXT (Why we do this)Our mobile app (SoulTune) is 100% Privacy-First and operates entirely offline. There are no user accounts, no logins, and no cloud-sync inside the app. The .soultune file is a proprietary ZIP archive containing the audio and a manifest.json. Once the user opens the file on their device, the app's internal engine handles the rest. Your only job is to get that file to their email inbox securely after a verified purchase.4. TASKS FOR YOU (The Agent)Initialize Project: Set up the Node.js/TypeScript project with Express.ClickBank Middleware: Write the exact cryptographic logic to verify the ClickBank IPN signature (HMAC-SHA1).Download Route: Implement a route to serve the local quantum_wealth.soultune file securely via a tokenized URL.Email Service: Implement the email dispatcher (Nodemailer setup).Deployment Script: Provide a brief ecosystem.config.js for PM2 so I can start the server on my VPS effortlessly.Focus on robust error handling. If a webhook fails, log it locally to a file so we can debug.